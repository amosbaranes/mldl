from django.apps import AppConfig

#
# class MlConfig(AppConfig):
#     name = 'ml'

class MlConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'academycity.apps.acapps.ml'  # Use the full path
